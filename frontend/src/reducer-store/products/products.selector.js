export const selectProductList = (state) => state.products.productsList;

export const selectLoadingStatus = (state) => state.products.loading;

export const selectErrorStatus = (state) => state.products.err;
export const selectProductsold = (state) => state.products.productsold;
